#include<bits/stdc++.h>
using namespace std;

int main() {
	int a1 = 3, r = 2;
	cout << size(double);
	long double l, n, an;
	while (scanf("%llf", &l)) {	
		if (l==0) break;
		n = l-2;
		an = a1 + (n-1) * r;
		
		double x = double(an - l)/l;
		
		printf("%.6lf\n", x);
	}
	return 0;
}
